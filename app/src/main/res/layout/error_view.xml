<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <ImageView
            android:id="@+id/img_emptyView"
            style="@style/empty_img"
            android:src="@drawable/ic_error"
            app:layout_constraintBottom_toTopOf="@+id/tv_emptyText"
            app:layout_constraintEnd_toEndOf="@+id/tv_emptyText"
            app:layout_constraintStart_toStartOf="@+id/tv_emptyText"
            tools:ignore="contentDescription" />

    <TextView
            android:id="@+id/tv_emptyText"
            android:text="@string/empty_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/txt_color"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.6" />

</androidx.constraintlayout.widget.ConstraintLayout>